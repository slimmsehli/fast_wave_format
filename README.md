# waveform converter from VCD to custom format 
* This is a custom Wavefome converter from the standard vcd file format to a custom fast and compressed format
* this uses compression on the signals and sperates the vcd dumped values on every chage into sperate columns for each signal  